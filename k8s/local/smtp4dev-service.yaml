apiVersion: v1
kind: Service
metadata:
  name: smtp4dev
  namespace: oficina
spec:
  type: ClusterIP
  selector:
    app: smtp4dev
  ports:
    - name: web
      port: 80
      targetPort: 80
    - name: smtp
      port: 25
      targetPort: 25
